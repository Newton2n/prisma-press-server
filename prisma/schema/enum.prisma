enum ActiveStatus {
    ACTIVE
    INACTIVE
    BLOCKED
}

enum Role {
    USER
    ADMIN
    AUTHOR
}

enum PostStatus {
    DRAFT
    PUBLISHED
    ARCHIVED
}

enum CommentStatus {
    APPROVED
    REJECT
}
